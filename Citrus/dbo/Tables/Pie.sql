CREATE TABLE [dbo].[Pie] (
    [PieId]    INT          IDENTITY (1, 1) NOT NULL,
    [Name]     VARCHAR (20) NOT NULL,
    [Rank]     INT          NOT NULL,
    [RecipeId] INT          NOT NULL
);
GO

